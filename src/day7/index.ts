function parseLine(line) {
  const commands = line.split(" ");
  if (command[0] === "$") {
  }
}

function run() {
  const lines = fs.readFileSync("./input.txt", "utf-8").split("\n");

  lines.forEach((line = {}));
}
